{
  "name": "starter-api",
  "version": "1.0.0",
  "description": "starter for REST services that work with PostgreSQL",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mvolkmann/starter-api.git"
  },
  "dependencies": {
    "body-parser": "^1.17.1",
    "express": "^4.15.2",
    "node-token-auth": "^1.0.0",
    "pg": "^6.1.3",
    "postgresql-easy": "^1.1.0",
    "socket.io": "^1.7.3"
  },
  "scripts": {
    "coverage": "cross-env CI=true npm test -- --coverage",
    "lint": "eslint --quiet src __tests__ --ext .js,.jsx",
    "start": "sudo nodemon lib/index.js",
    "test": "react-scripts test --env=jsdom",
    "verify": "npm run lint && cross-env CI=true npm test"
  },
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
